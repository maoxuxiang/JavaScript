/**
 * Contains configurable parameters.
 */

/**
 * max number of article to show
 */
var MAX_ARTICLE = 5;

/**
 * max number of articles to show when full window
 */
var MAX_ARTICLE_FULLWINDOW = 10;

/**
 * max number of sentences to show
 */
var MAX_SENTENCE = 10;

/**
 * User selected disease type
 */
var diseaseType = "";

/**
 * max number of sentences to show when full window
 */
var MAX_SENTENCE_FULLWINDOW = 10; 